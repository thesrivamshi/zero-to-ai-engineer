window.GLOSSARY = {
"token": "The chunk of text a model actually reads and writes — usually a word piece, about 4 characters or ¾ of a word in English. Models never see letters or words, only tokens.",
"tokenizer": "The program that splits text into tokens before it reaches the model, using a fixed vocabulary learned from data (commonly ~50k–200k pieces).",
"llm": "Large Language Model — a neural network with billions of parameters trained on huge amounts of text to predict the next token.",
"foundation model": "A big, general-purpose model (language, vision, or both) trained once at enormous cost, that everyone else builds applications on top of.",
"parameter": "One learned number inside a neural network (a 'weight'). A 7B model has 7 billion of them. Parameters ARE the model's knowledge.",
"weight": "Same as parameter — one of the billions of numbers a model learns during training.",
"pretraining": "The first, expensive training phase: the model reads trillions of tokens and learns to predict the next one. This is where raw capability comes from.",
"post-training": "Everything done after pretraining to make the model useful and safe: instruction tuning, RLHF/DPO, etc.",
"fine-tuning": "Continuing to train an existing model on your own (much smaller) dataset so it adapts to your task, style, or domain.",
"rlhf": "Reinforcement Learning from Human Feedback — training a model to prefer responses humans rate highly. A key reason chatbots feel helpful rather than like raw autocomplete.",
"dpo": "Direct Preference Optimization — a simpler, popular alternative to RLHF that trains directly on pairs of (preferred, rejected) answers.",
"sft": "Supervised Fine-Tuning — fine-tuning on example input→output pairs, e.g. (instruction, ideal answer).",
"lora": "Low-Rank Adaptation — a fine-tuning trick that trains tiny add-on matrices instead of all weights, cutting memory needs ~100× with little quality loss.",
"qlora": "LoRA applied on top of a quantized (compressed) base model, letting you fine-tune big models on a single modest GPU.",
"hallucination": "When a model states something false with full confidence. Not a bug in one model — a consequence of how next-token prediction works.",
"context window": "The maximum number of tokens a model can 'see' at once — its working memory. Everything outside it does not exist for the model.",
"prompt": "Everything you send to the model: instructions, examples, retrieved documents, the user's question. The model's entire universe for that request.",
"system prompt": "A special instruction block set by the developer (not the user) that defines the model's role, rules, and persona.",
"prompt engineering": "Systematically crafting and testing prompts to get reliable behavior from a model — the cheapest model-adaptation technique.",
"few-shot": "Putting a handful of worked examples in the prompt so the model imitates the pattern. Often dramatically better than instructions alone.",
"zero-shot": "Asking the model to do a task with instructions only, no examples.",
"chain-of-thought": "Prompting the model to reason step by step before answering. Improves accuracy on logic, math, and multi-step tasks.",
"temperature": "A dial controlling randomness when the model picks the next token. 0 = always the most likely token (predictable); higher = more diverse and creative, eventually chaotic.",
"sampling": "The act of choosing the next token from the model's probability distribution. Temperature, top-p, and top-k all shape this choice.",
"top-p": "Nucleus sampling — only consider the most likely tokens whose probabilities add up to p (e.g. 0.9), then sample among them.",
"inference": "Running a trained model to get outputs. Training happens once; inference happens on every single request — so its speed and cost dominate production.",
"api": "Application Programming Interface — a way for your code to talk to someone else's software over the internet. You send a request, you get a response.",
"api key": "A secret password-like string that identifies and bills your account when calling an API. Never share it or put it in code you publish.",
"embedding": "A list of numbers (a vector) representing the meaning of a piece of text. Texts with similar meaning get nearby vectors — the foundation of semantic search and RAG.",
"vector database": "A database built to store embeddings and find the nearest ones to a query vector extremely fast. Examples: Qdrant, Pinecone, pgvector, Chroma.",
"cosine similarity": "A score between -1 and 1 measuring whether two vectors point the same direction — the standard way to compare embeddings.",
"rag": "Retrieval-Augmented Generation — fetch relevant chunks of your own data and paste them into the prompt so the model answers from facts instead of memory.",
"chunking": "Splitting documents into pieces before embedding them for RAG. Chunk size is one of the most impactful RAG tuning knobs.",
"reranker": "A second, more careful model that re-sorts the top retrieval results by true relevance — a cheap way to boost RAG quality.",
"agent": "An LLM put in a loop where it can choose actions (search, run code, call APIs), observe results, and decide what to do next until the task is done.",
"function calling": "A model capability where it outputs a structured request to call one of the tools/functions you described, instead of plain text.",
"structured output": "Forcing the model to reply in a machine-readable format (usually JSON matching a schema) so other software can use the answer.",
"guardrails": "Checks around a model — on inputs (block injection, off-topic, PII) and outputs (block toxic, false, or malformed responses).",
"prompt injection": "An attack where malicious text in the input (e.g. inside a retrieved document or email) tricks the model into ignoring its instructions.",
"evaluation": "Measuring how good your AI system actually is — with exact checks, statistical metrics, human review, or LLM judges. The hardest and most important discipline in AI engineering.",
"llm-as-judge": "Using a strong model to grade another model's outputs against criteria. Scalable but has biases that must themselves be evaluated.",
"benchmark": "A standard test set (like MMLU or HumanEval) used to compare models. Useful for ballpark, dangerous to over-trust — public benchmarks leak into training data.",
"ground truth": "The known-correct answer you compare a model's output against during evaluation.",
"latency": "How long a request takes. For LLMs split into TTFT (time to first token) and per-token speed afterwards.",
"ttft": "Time To First Token — how long the user waits before anything appears. Dominated by the prefill phase processing the prompt.",
"tpot": "Time Per Output Token — how fast tokens stream once they start. Dominated by memory bandwidth during decode.",
"throughput": "Total work a system completes per second (e.g. tokens/sec across all users). Throughput pays the bills; latency keeps users happy. They trade off.",
"prefill": "The first inference phase: the model processes the whole prompt in parallel. Compute-bound — fast per token, but proportional to prompt length.",
"decode": "The second inference phase: generating tokens one at a time. Memory-bandwidth-bound — the GPU mostly waits for weights to load from memory.",
"kv cache": "Stored attention keys/values for all previous tokens, so each new token doesn't recompute the past. Saves massive compute, costs GPU memory.",
"prefix caching": "Reusing the KV cache across requests that share an identical beginning (like a long system prompt) — huge savings for chat apps.",
"batching": "Processing many users' requests in one GPU pass. The single most important throughput technique in serving.",
"continuous batching": "Smart batching where finished requests leave the batch and new ones join mid-flight, instead of waiting for the slowest request.",
"quantization": "Storing weights in fewer bits (16→8→4) to shrink memory and speed up inference, at a small quality cost.",
"speculative decoding": "A small 'draft' model guesses several tokens ahead; the big model verifies them all in one pass. Same output, fewer slow steps.",
"tensor parallelism": "Splitting each layer of a model across multiple GPUs that work together on every token — used when the model doesn't fit on one GPU or for lower latency.",
"gpu": "Graphics Processing Unit — the chip AI runs on, built to do thousands of multiplications in parallel. Its memory size and memory bandwidth are the key specs for inference.",
"vram": "A GPU's onboard memory. The model weights AND the KV cache must fit in it. The scarcest resource in inference.",
"memory bandwidth": "How fast data moves between GPU memory and compute units. LLM decoding speed is usually limited by this, not by raw compute.",
"vllm": "The most popular open-source inference engine. Introduced PagedAttention for efficient KV-cache memory management and continuous batching.",
"inference engine": "Specialized serving software (vLLM, SGLang, TensorRT-LLM) that runs models far more efficiently than naive code — batching, caching, optimized kernels.",
"mlops": "The discipline of running ML systems in production: pipelines, versioning, deployment, monitoring, automation.",
"llmops": "MLOps specialized for LLM applications: prompt versioning, token cost tracking, output monitoring, eval pipelines, feedback loops.",
"pipeline": "An automated sequence of steps (collect data → process → embed → store, or train → evaluate → deploy) that runs without a human babysitting it.",
"orchestrator": "A tool (like ZenML, Airflow, Prefect) that schedules and tracks pipeline steps, retries failures, and records what ran when.",
"feature pipeline": "In the FTI design: the pipeline that turns raw data into model-ready features (for LLM apps: cleaned, chunked, embedded documents).",
"model registry": "A versioned store for trained models — like git for model weights — so you always know exactly which model is deployed.",
"docker": "A tool that packages your app with everything it needs into a 'container' that runs identically anywhere — the standard unit of deployment.",
"autoscaling": "Automatically adding or removing servers/GPUs as traffic changes, so you neither melt under load nor pay for idle hardware.",
"cold start": "The delay when a new server spins up (loading a multi-GB model into GPU memory can take minutes) — a major autoscaling pain for AI services.",
"model cascade": "Routing easy requests to a cheap model and hard ones to an expensive model — often cuts costs 5–10× with minimal quality loss.",
"distillation": "Training a small model to imitate a big one's outputs — keeping much of the quality at a fraction of the inference cost.",
"moe": "Mixture of Experts — an architecture where only a fraction of the model's parameters activate per token, giving big-model quality at lower compute per token.",
"transformer": "The neural network architecture behind modern LLMs, built around the attention mechanism. Introduced in 2017 ('Attention Is All You Need').",
"attention": "The mechanism letting each token look at every other token and decide which ones matter for predicting what comes next.",
"open weights": "Models whose trained weights you can download and run yourself (Llama, Mistral, Qwen) — vs closed models accessible only through an API.",
"json": "A simple text format for structured data, e.g. {\"name\": \"Ada\", \"age\": 36}. The lingua franca of APIs.",
"terminal": "The text-based window where you type commands to your computer directly. AI engineers live here. Also called command line, console, or shell.",
"python": "The programming language of AI. Readable, beginner-friendly, and used by every major AI library.",
"virtual environment": "An isolated folder of Python packages for one project, so projects don't break each other. Created with 'python -m venv'.",
"pip": "Python's package installer — 'pip install openai' downloads a library so your code can use it.",
"streaming": "Sending the model's answer token-by-token as it's generated instead of waiting for the whole thing — what makes chatbots feel fast.",
"observability": "Being able to see what your production system is doing: logs, traces of every LLM call, latencies, costs, error rates, output quality.",
"self-supervision": "A way to train a model without human-labeled data: the data labels itself. In language modeling, each text sequence supplies both the input (earlier tokens) and the answer (the next token), so trillions of tokens of raw text become free training examples.",
"supervision": "Training a model on data that humans have labeled (e.g. each image tagged 'cat' or 'dog'). Accurate but expensive and slow, because every example needs a human label.",
"masked language model": "A model trained to fill in blanks anywhere in a sequence, using context from both sides (e.g. BERT). Good for understanding tasks like classification; not used for open-ended generation.",
"autoregressive": "A model that generates one token at a time, each conditioned only on the tokens before it. All modern text-generating LLMs are autoregressive. Also called 'causal'.",
"multimodal": "A model that works with more than one kind of data — text plus images, audio, or video. A large multimodal model (LMM) generates tokens conditioned on all the modalities it supports.",
"generative": "Able to produce open-ended outputs (text, images, audio) rather than only picking from a fixed set of labels. The 'generative' in generative AI.",
"vocabulary": "The complete fixed set of tokens a model can read or produce — typically 32k–200k pieces. Decided by the model's developers when the tokenizer is built.",
"scaling laws": "The empirically observed, smooth relationship in which model quality improves predictably as you increase parameters, training data, and compute together. The reason the field bet on 'bigger'.",
"compute-optimal": "The best split of a fixed training budget between model size and data size. The Chinchilla result showed many large models were undertrained — given the compute, they should have used far more data.",
"emergence": "When a capability is absent in smaller models and appears, sometimes abruptly, only past a certain scale. Makes the exact abilities of the next model hard to predict.",
"mixture of experts": "An architecture that splits the network into many 'expert' sub-networks and, for each token, activates only a few. You get the knowledge capacity of a huge model while paying compute for only a small slice. Mixtral and many frontier models use it.",
"top-k": "A sampling rule that keeps only the k most likely next tokens and discards the rest before rolling the weighted die. Caps how adventurous generation can get.",
"greedy decoding": "Always picking the single most likely next token (temperature 0). Deterministic and safe, but often dull and prone to loops.",
"test-time compute": "Spending extra computation at generation time to get better answers — e.g. sampling several candidates and picking the best, or letting the model reason step by step. Trades money/latency for quality.",
"model as a service": "Accessing a model through a hosted API you call over the internet, paying per token, instead of running the model yourself. The approach OpenAI popularized; it dropped the barrier to building AI apps.",
"human-in-the-loop": "Keeping a person in the AI's decision path — reviewing, approving, or handling the hard cases. A core way to ship AI safely before it's trusted to act alone.",
"data flywheel": "A self-reinforcing loop where using a product generates data that improves the product, which attracts more use, which generates more data. The data moat AI startups chase.",
"last mile": "The hard final stretch of an AI product: going from an impressive 80%-good demo to a reliable 95%+ product. Usually takes far longer than the demo did.",
"throughput-vs-latency": "The central serving trade-off: latency is how fast one user gets their answer; throughput is how many users you serve per second. Batching raises throughput at some cost to latency.",
"natural language supervision": "Training a model using naturally occurring text paired with other data (e.g. image-caption pairs scraped from the web) as the supervision signal, avoiding manual labeling. How CLIP was trained.",
"embedding model": "A model trained to turn data (text, an image) into a vector that captures its meaning, so similar things land near each other. The backbone of search and RAG; CLIP is a text-image example.",
"git": "The standard version-control tool: it records snapshots ('commits') of your project over time so you can see history, undo mistakes, and collaborate. Runs locally; works with or without a hosting service like GitHub.",
"commit": "A saved snapshot of your project at a moment in time, with a message describing what changed. The basic unit of git history — you can always return to any past commit.",
"repository": "A project tracked by git (a 'repo') — your files plus the full history of commits. Created with `git init` or cloned from a host like GitHub.",
"staging area": "Git's holding zone: `git add` moves changes into the staging area, and `git commit` snapshots exactly what's staged. It lets you choose precisely which changes go into the next commit.",
"github": "A website that hosts git repositories in the cloud, so you can back up your code, share it, and collaborate. Git is the tool; GitHub is one popular place to put repos.",
"gitignore": "A file named .gitignore listing paths git should never track — secrets (.env), virtual environments (venv/), caches. The first line of defense against committing API keys.",
"remote": "A copy of your repository hosted elsewhere (e.g. on GitHub), nicknamed 'origin' by convention. `git push` sends your commits to it; `git pull` brings others' commits down.",
"perplexity": "A measure of how 'surprised' a language model is by a piece of text — roughly, how many equally-likely tokens it was choosing among. Lower perplexity = the model predicts the text better. A classic intrinsic quality metric.",
"bleu": "An early automatic text-overlap score (originally for translation) that compares a model's output to reference text by matching word sequences (n-grams). Cheap but blind to meaning — paraphrases score low.",
"rouge": "A text-overlap score family (popular for summarization) measuring how much of the reference text's words/phrases the output recovers. Like BLEU, it counts overlap, not meaning.",
"semantic similarity": "Comparing two texts by the closeness of their embeddings (meaning) rather than exact word overlap — so 'the cost is high' and 'it's expensive' score as similar. Used to score open-ended answers.",
"pairwise comparison": "Evaluating by asking which of two outputs is better, rather than scoring each alone. Humans (and judge models) are more consistent at comparisons than absolute scores; the basis of ranking and arenas.",
"benchmark contamination": "When a benchmark's test questions leaked into a model's training data, so high scores reflect memorization, not ability. A core reason public benchmark numbers can mislead.",
"test set": "A fixed collection of inputs (often with known good answers) you run your system against to measure quality. Building and growing a representative test set is the heart of real evaluation.",
"regression": "When a change that improves one thing silently breaks another. Re-running your eval set after every change catches regressions before users do.",
"json mode": "An API setting that forces the model's output to be valid JSON, so your code can parse it reliably. A practical form of structured output.",
"stateless": "Having no memory between requests. The chat API is stateless: the server forgets you after each call, so YOU must resend the whole conversation every time to give the model context.",
"jailbreak": "A prompt crafted to trick a model past its safety training into doing what it's supposed to refuse. Related to prompt injection but aimed at the model's guardrails rather than an app's instructions.",
"self-consistency": "A prompting technique: sample several chain-of-thought answers at higher temperature, then take the majority vote. Trades extra tokens/cost for accuracy on reasoning tasks.",
"delimiter": "A clear marker (triple quotes, XML-style tags, ###) that separates instructions from data in a prompt, so the model can't confuse the two. A core defense against prompt injection and a clarity win.",
"bm25": "A classic keyword-based retrieval algorithm that ranks documents by how often the query's words appear (weighted so rare words count more). Fast, no model needed, and still strong — especially for exact terms, names, and codes.",
"sparse retrieval": "Retrieval based on word/keyword matching (e.g. BM25), where documents are represented by which terms they contain. Great at exact matches, blind to synonyms and meaning.",
"dense retrieval": "Retrieval based on embeddings, where documents are dense vectors of meaning. Matches by semantic similarity, so it finds relevant text even with no shared words — the opposite strengths of sparse retrieval.",
"hybrid search": "Combining keyword (sparse/BM25) and embedding (dense) retrieval and merging their results, to get exact-match precision AND semantic recall. The default for serious RAG systems.",
"recall@k": "Of all the documents that should have been retrieved for a query, what fraction appeared in the top k results. Measures whether retrieval found the right context — the metric that most determines RAG quality.",
"precision@k": "Of the top k retrieved documents, what fraction are actually relevant. High precision means little junk in the context; balanced against recall.",
"faithfulness": "Whether a generated answer is actually supported by the retrieved context, rather than invented. The key generation-side metric for RAG: an answer can be fluent and well-retrieved yet still unfaithful to its sources.",
"query rewriting": "Transforming the user's raw question into a better search query before retrieval — fixing typos, resolving pronouns from chat history, or making a vague question specific. Often the cheapest RAG quality win.",
"query expansion": "Broadening a query with synonyms, related terms, or multiple rephrasings (sometimes several generated variants) to retrieve more of the relevant documents. Raises recall at some cost to precision.",
"self-query": "Letting the model turn a natural-language question into a structured query — extracting filters (date, author, category) to combine with semantic search, so 'docs from 2023 about refunds' filters by year AND matches meaning.",
"react": "An agent pattern (Reason+Act) where the model alternates between reasoning steps and tool actions: think, call a tool, observe the result, think again — looping until it can answer. The backbone of most tool-using agents.",
"reflection": "An agent technique where the model reviews its own output or plan, critiques it, and revises — catching mistakes a single pass would miss. Trades extra calls for reliability.",
"agent memory": "How an agent retains information beyond one context window — short-term (the running transcript) and long-term (notes/facts saved to a store and retrieved later), so it can work across many steps or sessions.",
"change data capture": "A pattern for keeping a derived store (like a vector DB) in sync with a source: detect what changed at the source and propagate only those changes, instead of rebuilding everything. Keeps RAG indexes fresh automatically.",
"inference pipeline": "The serving-time path of an ML/RAG system: take a request, rewrite/expand the query, retrieve and rerank context, assemble the prompt, call the model, post-process. The counterpart to the offline feature pipeline.",
"moderation": "Automated screening of input or output for disallowed content (hate, self-harm, violence, etc.), often via a dedicated classification endpoint. A standard input/output guardrail.",
"pii": "Personally Identifiable Information — names, emails, phone numbers, IDs. A guardrail concern: detect and redact it before logging, before sending to a model, or before returning a response.",
"ground truth retrieval set": "For evaluating retrieval, the human-labeled set of which documents are actually relevant to each test query — what recall@k and precision@k are measured against.",
"instruction dataset": "A training set of (instruction → ideal response) examples, usually formatted as chat messages, used for supervised fine-tuning to teach a model to follow instructions in a particular way or voice.",
"chat template": "The exact text format (special tokens and role markers) a chat model expects, e.g. wrapping each turn with role tags. You must apply the model's own chat template when preparing fine-tuning data, or training silently degrades.",
"sequence packing": "Concatenating several short training examples into one full-length sequence (separated by markers) so no tokens are wasted on padding — making fine-tuning faster and cheaper without changing what's learned.",
"model merging": "Combining the weights of two or more models (or adapters) into one — e.g. averaging them or merging task-specific fine-tunes — to blend capabilities without retraining. Cheap, surprisingly effective, and a bit alchemical.",
"data decontamination": "Removing from training data any examples that overlap with your evaluation/benchmark sets, so test scores measure real ability rather than memorization. The flip side of benchmark contamination.",
"preference dataset": "A dataset of (prompt, chosen, rejected) triples expressing which response is better, used to align a model with DPO or RLHF. Built from human ratings, production thumbs up/down, or LLM-judged comparisons.",
"reward model": "In RLHF, a separate model trained on human preference data to score how good a response is; the LLM is then optimized to maximize that score. DPO removes the need for an explicit reward model.",
"web scraping": "Programmatically extracting text/data from web pages (vs. a clean API). A common raw-data source for building datasets; needs cleaning, deduplication, and attention to terms of service and legality.",
"data pipeline": "An automated, staged process that turns raw sources into clean, structured data ready for use (ingest → clean → transform → store). The same idea as a RAG feature pipeline, generalized to dataset building.",
"fti": "Feature/Training/Inference — the LLM Engineer's Handbook architecture that splits an ML system into three independent pipelines: a feature pipeline (prepare data), a training pipeline (produce models), and an inference pipeline (serve requests). They communicate through shared stores, not direct calls.",
"cors": "Cross-Origin Resource Sharing — a browser security rule that blocks a web page from reading responses from a different domain unless the server explicitly allows it with Access-Control-Allow-Origin headers. You must enable CORS for a browser app to call your API.",
"real-time inference": "Serving a model synchronously, returning an answer while the user waits (a chatbot reply). Latency-sensitive; the default for interactive apps.",
"batch inference": "Running a model over a large set of inputs offline, all at once, when no one is waiting — e.g. classifying a million documents overnight. Optimizes throughput and cost over latency.",
"async inference": "Accepting a request, doing the work in the background, and returning the result later (via a callback, queue, or polling). For jobs too slow for real-time but that don't fit a fixed batch window.",
"ci/cd": "Continuous Integration / Continuous Delivery — automation that tests every code change and ships it through to production safely. For ML it also covers testing data, models, and evals, not just code.",
"canary deployment": "Releasing a change to a small slice of traffic first, watching metrics, then rolling it out fully if healthy (or rolling back if not). A safe way to ship risky changes.",
"blue-green deployment": "Running two production environments (blue = current, green = new); you switch traffic to green once it's verified, and can instantly switch back. Enables zero-downtime deploys and instant rollback.",
"scale-to-zero": "Shutting a service down to zero running instances when idle (so it costs nothing) and starting one on the next request. Saves money for spiky traffic, at the price of a cold-start delay.",
"api gateway": "A single entry point in front of your services that handles cross-cutting concerns — auth, rate limiting, routing, logging — so each service doesn't reimplement them. An 'LLM gateway' adds model routing, caching, and guardrails.",
"artifact": "A versioned output of an ML pipeline — a dataset, a trained model/adapter, an index, an eval report. Tracking artifacts and their lineage is how you reproduce and roll back a system.",
"explicit feedback": "Signals a user deliberately gives about output quality — thumbs up/down, ratings, corrections, a regenerate click. Direct but sparse (most users don't bother).",
"implicit feedback": "Signals inferred from behavior rather than stated — did they copy the answer, retry, abandon, click the cited source? Plentiful and noisy; the backbone of a real feedback system.",
"arithmetic intensity": "The ratio of compute (FLOPs) to memory traffic (bytes moved) for an operation. Compare it to the hardware's ops:byte ratio to know whether you're compute-bound or memory-bound.",
"ops:byte": "A GPU's ratio of peak compute (FLOP/s) to memory bandwidth (bytes/s). An operation whose arithmetic intensity is below this ratio is starved for data (memory-bound); above it, it's compute-bound. The roofline idea.",
"memory-bound": "When a computation spends most of its time waiting for data to move from memory, leaving the math units idle. LLM decoding at small batch sizes is memory-bound — which is why batching and quantization help so much.",
"compute-bound": "When a computation is limited by the speed of the math units rather than memory movement. LLM prefill, and decode at large batch sizes, tend toward compute-bound.",
"flashattention": "A fused, memory-aware implementation of attention that avoids writing the huge intermediate attention matrix to slow GPU memory, computing it in fast on-chip memory instead. Makes attention far faster and lower-memory without changing the result.",
"kernel": "A function that runs on the GPU. Deep-learning frameworks launch many kernels; how efficiently they use the hardware largely determines speed.",
"kernel fusion": "Combining several GPU operations into one kernel so intermediate results stay in fast on-chip memory instead of being written to and read from slow memory between steps. A primary source of inference speedups.",
"cuda": "NVIDIA's platform and language for programming GPUs. The inference engines and libraries you use are built on CUDA kernels under the hood.",
"roofline": "A model of performance that plots achievable throughput against arithmetic intensity: a sloped 'memory' region (bandwidth-limited) rising to a flat 'compute' ceiling. Tells you which resource is the bottleneck and whether optimization is worth it.",
"tensor parallelism": "Splitting each layer's matrices across multiple GPUs that compute one layer together and synchronize. Cuts per-GPU memory and speeds each layer, but needs very fast interconnect, so it's used within one machine.",
"pipeline parallelism": "Splitting a model's layers into consecutive stages on different GPUs, so requests flow through like an assembly line. Low communication (spans machines), but single requests leave stages idle ('bubbles') unless many are pipelined.",
"expert parallelism": "Spreading a Mixture-of-Experts model's experts across GPUs, routing each token to the GPUs holding its chosen experts. Fits huge expert counts but can imbalance load.",
"disaggregated serving": "Running the prefill phase and the decode phase on separate pools of GPUs, since they have different performance profiles (prefill is compute-heavy, decode memory-heavy). Lets each be scaled and optimized independently.",
"continuous batching": "An inference-server technique that adds and removes requests from the running batch every step, instead of waiting for a whole batch to finish. Keeps the GPU busy and dramatically raises throughput; pioneered in serving engines like vLLM.",
"paged attention": "A KV-cache memory technique (from vLLM) that stores the cache in non-contiguous fixed-size blocks like virtual-memory pages, eliminating fragmentation so far more concurrent requests fit in GPU memory.",
"load testing": "Sending controlled, increasing traffic at a service to measure how throughput, latency percentiles, and errors behave under load — and to find the point where it breaks. Essential before trusting a serving setup.",
"vlm": "Vision-Language Model — a model that takes images (and text) as input and produces text, e.g. describing or answering questions about a picture. Serving it adds an image-encoding step before the language model.",
"asr": "Automatic Speech Recognition — turning audio speech into text (speech-to-text). One of the modalities you may need to serve alongside LLMs.",
"tts": "Text-to-Speech — generating spoken audio from text. Often latency-sensitive and streamed, with its own serving considerations.",
"zero-downtime deploy": "Releasing a new version with no interruption to users, typically via blue-green or rolling strategies that shift traffic to healthy new instances before retiring old ones."
};
